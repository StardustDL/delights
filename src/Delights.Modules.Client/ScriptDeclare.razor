@inject ModuleCollection Collection
@inject IServiceProvider ServiceProvider

@foreach (ClientModule c in Collection.Modules)
{
    foreach (var r in c.GetUI(ServiceProvider).Resources)
    {
        if (r.Type is UIResourceType.Script)
        {
            @((MarkupString)($"<script type=\"text/javascript\" src=\"{r.Path}\" {Delights.Modules.Client.CoreClientModuleUI.ResourceTagAttrName}=\"{r.Path}\"></script>"))
        }
    }
}
