@inject ModuleCollection Collection
@inject IServiceProvider ServiceProvider

@foreach (ClientModule c in Collection.AllClientModules())
{
    foreach (var r in c.GetUI(ServiceProvider).Resources)
    {
        if (r.Type is UIResourceType.Script)
        {
            @((MarkupString)($"<script type=\"text/javascript\" src=\"{r.Path}\" {Delights.Modules.Client.Core.ModuleUI.ResourceTagAttrName}=\"{r.Path}\"></script>"))
        }
    }
}
